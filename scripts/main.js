(function (MMC, undefined) {
  "use strict";



  //var testActor = new MMC.Objects.Actor();
  //propertyViewer.displayProperties(testActor);

  // Create the game and start it!
  MMC.theGame = new MMC.Game();
  MMC.theGame.runGame();

}(window.MMC = window.MMC || {}));